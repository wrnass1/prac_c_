#include <stdlib.h>
#include <stdio.h>
#include <sys/types.h>

int main(void){
    // system("sleep 5 &");
    // printf("end-of-program\n");
    // return 0;

    // system("cd somewhere");
    // return 0;

    // int uid = getuid();
    // struct passwd * pwdp = getpwuid(uid);
    // if (pwdp == NULL){
    //     fprintf()
    // }

    fork();
    printf("Hello World!\n");
    sleep(15);
    return 0;

}